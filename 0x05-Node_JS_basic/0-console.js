// Create a function named displayMessage that prints in STDOUT the string argument.
function displayMessage (data) {
  console.log(data);
}

// Export the displayMessage function explicitly
module.exports = displayMessage;
